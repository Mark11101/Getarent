import { StyleSheet } from "react-native";

export default StyleSheet.create({
    depositModal: {
        flex: 1,
        padding: 20,
        paddingBottom: 80,
    },
    depositModalText: {
        fontSize: 16,
        lineHeight: 24,
    },
});
