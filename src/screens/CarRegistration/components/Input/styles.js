import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
		marginBottom: 20,
    },
    labelStyle: {
        marginBottom: 5,
        fontWeight: 'bold',
    },
    inputStyle: {
        fontWeight: 'bold',
    },
});
